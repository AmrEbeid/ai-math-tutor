import { Feature } from '@zeluu/design-system';

export const MathHelp = () => (
  <div style={{ maxWidth: 520 }}>
    <Feature icon="🧮" title="Step-by-step math help">
      Your child gets guided, age-appropriate explanations for every problem —
      from grade 1 counting to grade 9 algebra — never just the final answer.
    </Feature>
  </div>
);

export const ExamPrep = () => (
  <div style={{ maxWidth: 520 }}>
    <Feature icon="📝" title="Exam &amp; homework prep">
      Practice quizzes and worked examples for Math, Science and English, matched
      to your child's grade and the topics their teacher is covering.
    </Feature>
  </div>
);

export const ParentControls = () => (
  <div style={{ maxWidth: 520 }}>
    <Feature icon="🛡️" title="Parent-controlled &amp; safe">
      You set the subjects, see every session, and manage credits. No ads, no
      data sold, and child chats are never used to train AI.
    </Feature>
  </div>
);
